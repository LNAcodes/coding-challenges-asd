info:
  name: GetOneComment
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/comments/:id/"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
