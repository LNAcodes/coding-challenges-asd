info:
  name: Invalid UUID
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseURL}}threads/not-a-uuid"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
