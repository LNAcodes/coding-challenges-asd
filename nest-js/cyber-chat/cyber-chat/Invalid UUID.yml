info:
  name: Invalid UUID
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseURL}}threads/h492j20426"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
