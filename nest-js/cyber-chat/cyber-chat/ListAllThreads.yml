info:
  name: ListAllThreads
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}threads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
