info:
  name: CreateThreadWithTitleAndBody
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}threads"
  body:
    type: json
    data: |-
      {
        "title": "Erster Thread",
        "body": "Das ist ein Testthread",
        "author": "lna"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
