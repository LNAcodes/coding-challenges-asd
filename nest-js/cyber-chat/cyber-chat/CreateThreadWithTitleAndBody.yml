info:
  name: CreateThreadWithTitleAndBody
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}/threads"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
