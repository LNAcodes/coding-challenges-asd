info:
  name: CreateThreadWithTitleAndBody
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseURL}}threads"
  body:
    type: json
    data: |-
      {
        "title": "Test",
        "body": "Hallo"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
