info:
  name: DeleteThreadWithComments
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{baseURL}}/threads/:id/"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
