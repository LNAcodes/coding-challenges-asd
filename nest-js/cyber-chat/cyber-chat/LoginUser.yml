info:
  name: LoginUser
  type: http
  seq: 12

http:
  method: POST
  url: "{{baseURL}}auth/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
