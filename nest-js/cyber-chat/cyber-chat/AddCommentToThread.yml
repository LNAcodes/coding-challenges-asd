info:
  name: AddCommentToThread
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}/threads/:id/comments"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
