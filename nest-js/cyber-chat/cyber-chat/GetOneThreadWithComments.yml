info:
  name: GetOneThreadWithComments
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/threads/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
