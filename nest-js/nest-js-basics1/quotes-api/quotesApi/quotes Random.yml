info:
  name: quotes Random
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}/quotes/random"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
