info:
  name: Untitled 1
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}/quotes?author=Steve Jobs"
  params:
    - name: author
      value: Steve Jobs
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
